[[ $OSTYPE =~ "darwin" ]] || return

zplug "junegunn/fzf-bin", \
    from:gh-r, \
    as:command, \
    rename-to:fzf, \
    use:"*darwin*amd64*", \
	hook-load:"source $ZDOTDIR/zsh.rcs/fzf"

zplug "sharkdp/fd", \
    from:gh-r, \
    as:command, \
    use:"*apple*darwin*", \

